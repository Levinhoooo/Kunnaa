name: GetScreen Windows (4H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=afzanraisa@gmail.com" > secrets.txt
         Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/16xylqtz9xlapl177g2zm/down.bat?rlkey=lw2h4c96mpgk3zboad8dw24mi&dl=0" -OutFile "down.bat"
         cmd /c down.bat
